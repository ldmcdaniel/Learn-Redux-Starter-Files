import React from 'react';

const Comments = React.createClass({
    render() {
        return (
            <div className="comment">
                This is a comment
            </div>
        );
    }
});

export default Comments;
